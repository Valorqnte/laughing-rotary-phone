# 后端示例环境变量（把本文件复制为 .env 或在服务器上用 systemd/pm2 注入）

# 服务端口
PORT=3000

# MySQL
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=bookapp
DB_PASSWORD=lalala123
DB_NAME=book

# MinIO
MINIO_ENDPOINT=127.0.0.1
MINIO_PORT=9000
MINIO_USE_SSL=false
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin123
MINIO_BUCKET=book-files
# 是否允许后端自动创建 bucket（生产建议先手动创建，保持为 false）
MINIO_AUTO_CREATE_BUCKET=false

# bcrypt
BCRYPT_SALT_ROUNDS=10

# CORS（生产建议填你的域名；多个用逗号分隔）
# CORS_ORIGIN=https://example.com,https://www.example.com

